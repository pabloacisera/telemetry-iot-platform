# Ansible inventory for deployment
# Replace with your actual EC2 IP/hostname and key path

[production]
ec2-host ansible_host=<EC2_PUBLIC_IP> ansible_user=ec2-user ansible_ssh_private_key_file=~/.ssh/your-key.pem
