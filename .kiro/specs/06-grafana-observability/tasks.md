# Spec 06 — Tasks
1. MySQL datasource provisioning as code.
2. "Historical per motor" dashboard over `readings_hourly_agg`.
3. "Alerts by severity" dashboard over `alerts`/`sensor_faults`.
4. Verify both survive a `docker compose down/up` (persistent provisioning, not manual).
