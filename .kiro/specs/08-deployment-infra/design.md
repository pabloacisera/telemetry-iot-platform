# Spec 08 — Design
See `docs/10-deployment.md` for the full detail (manual and Ansible). The project's compose lives at
`/opt/<project-name>/docker-compose.yml`; the Nginx config specific to this app at
`/home/ec2-user/global/nginx/conf.d/<app>.conf`.
